# Review package: 52f57e84c74b7aea17fd6b34e80140f473d6be6e..HEAD

## Commits
9681bfb feat: add SidebarSyncCard and SidebarProfile components

## Files changed
 src/lib/components/SidebarProfile.svelte   | 67 ++++++++++++++++++++++++++++++
 src/lib/components/SidebarProfile.test.ts  | 21 ++++++++++
 src/lib/components/SidebarSyncCard.svelte  | 65 +++++++++++++++++++++++++++++
 src/lib/components/SidebarSyncCard.test.ts | 13 ++++++
 4 files changed, 166 insertions(+)

## Diff
diff --git a/src/lib/components/SidebarProfile.svelte b/src/lib/components/SidebarProfile.svelte
new file mode 100644
index 0000000..3de1151
--- /dev/null
+++ b/src/lib/components/SidebarProfile.svelte
@@ -0,0 +1,67 @@
+<script lang="ts">
+	import Icon from './Icon.svelte';
+
+	interface Props {
+		collapsed: boolean;
+	}
+
+	let { collapsed }: Props = $props();
+</script>
+
+<div class="profile" class:collapsed>
+	<div class="avatar">J</div>
+	{#if !collapsed}
+		<div class="details">
+			<div class="name">Jonas</div>
+			<div class="rating">Rapid <span class="sbmono rating-value">1867</span></div>
+		</div>
+		<Icon d="M6 9l6 6 6-6" size={15} stroke="#565C6B" strokeWidth={2} />
+	{/if}
+</div>
+
+<style>
+	.profile {
+		display: flex;
+		align-items: center;
+		gap: 10px;
+		padding: 9px;
+		border-radius: 12px;
+		background: var(--color-panel-bg);
+		border: 1px solid var(--color-hairline-low);
+	}
+	.profile.collapsed {
+		justify-content: center;
+		padding: 6px 0;
+		background: none;
+		border: none;
+	}
+	.avatar {
+		width: 34px;
+		height: 34px;
+		flex: none;
+		border-radius: 9px;
+		background: linear-gradient(135deg, #3b82f6, #a78bfa);
+		display: flex;
+		align-items: center;
+		justify-content: center;
+		font-weight: 700;
+		font-size: 14px;
+		color: #fff;
+		box-shadow: 0 4px 12px rgba(99, 102, 241, 0.35);
+	}
+	.details {
+		flex: 1;
+		min-width: 0;
+	}
+	.name {
+		font-size: 12.5px;
+		font-weight: 600;
+	}
+	.rating {
+		font-size: 10.5px;
+		color: var(--color-text-muted-dark);
+	}
+	.rating-value {
+		color: var(--color-light-green-1);
+	}
+</style>
diff --git a/src/lib/components/SidebarProfile.test.ts b/src/lib/components/SidebarProfile.test.ts
new file mode 100644
index 0000000..b1b2c4e
--- /dev/null
+++ b/src/lib/components/SidebarProfile.test.ts
@@ -0,0 +1,21 @@
+import { describe, it, expect } from 'vitest';
+import { render } from '@testing-library/svelte';
+import SidebarProfile from './SidebarProfile.svelte';
+
+describe('SidebarProfile', () => {
+	it('renders the name and rating when expanded', () => {
+		const { getByText } = render(SidebarProfile, { props: { collapsed: false } });
+		expect(getByText('Jonas')).toBeTruthy();
+		expect(getByText('1867')).toBeTruthy();
+	});
+
+	it('hides the name and rating when collapsed', () => {
+		const { queryByText } = render(SidebarProfile, { props: { collapsed: true } });
+		expect(queryByText('Jonas')).toBeNull();
+	});
+
+	it('always renders the avatar initial', () => {
+		const { getByText } = render(SidebarProfile, { props: { collapsed: true } });
+		expect(getByText('J')).toBeTruthy();
+	});
+});
diff --git a/src/lib/components/SidebarSyncCard.svelte b/src/lib/components/SidebarSyncCard.svelte
new file mode 100644
index 0000000..8c9ccff
--- /dev/null
+++ b/src/lib/components/SidebarSyncCard.svelte
@@ -0,0 +1,65 @@
+<div class="sync-card">
+	<div class="sync-header">
+		<span class="sync-label">Chess.com sync</span>
+		<span class="sync-status"><span class="sync-dot"></span>Synced</span>
+	</div>
+	<div class="sync-progress-track">
+		<div class="sync-progress-fill"></div>
+	</div>
+	<div class="sync-footer">
+		<span>247 games indexed</span>
+		<span class="sbmono">2m ago</span>
+	</div>
+</div>
+
+<style>
+	.sync-card {
+		background: #12141d;
+		border: 1px solid rgba(255, 255, 255, 0.06);
+		border-radius: 13px;
+		padding: 12px;
+		margin-bottom: 12px;
+	}
+	.sync-header {
+		display: flex;
+		align-items: center;
+		justify-content: space-between;
+		margin-bottom: 9px;
+	}
+	.sync-label {
+		font-size: 11px;
+		color: var(--color-text-tertiary);
+		font-weight: 500;
+	}
+	.sync-status {
+		font-size: 10px;
+		color: var(--color-accent-green);
+		display: flex;
+		align-items: center;
+		gap: 4px;
+	}
+	.sync-dot {
+		width: 5px;
+		height: 5px;
+		border-radius: 50%;
+		background: var(--color-accent-green);
+	}
+	.sync-progress-track {
+		height: 5px;
+		border-radius: 10px;
+		background: #1e2130;
+		overflow: hidden;
+		margin-bottom: 7px;
+	}
+	.sync-progress-fill {
+		width: 100%;
+		height: 100%;
+		background: linear-gradient(90deg, #2de0ce, #4adea0);
+	}
+	.sync-footer {
+		display: flex;
+		justify-content: space-between;
+		font-size: 10px;
+		color: var(--color-text-muted-dark);
+	}
+</style>
diff --git a/src/lib/components/SidebarSyncCard.test.ts b/src/lib/components/SidebarSyncCard.test.ts
new file mode 100644
index 0000000..f4eb4ac
--- /dev/null
+++ b/src/lib/components/SidebarSyncCard.test.ts
@@ -0,0 +1,13 @@
+import { describe, it, expect } from 'vitest';
+import { render } from '@testing-library/svelte';
+import SidebarSyncCard from './SidebarSyncCard.svelte';
+
+describe('SidebarSyncCard', () => {
+	it('renders the sync status and stats from README §6.1', () => {
+		const { getByText } = render(SidebarSyncCard);
+		expect(getByText('Chess.com sync')).toBeTruthy();
+		expect(getByText('Synced')).toBeTruthy();
+		expect(getByText('247 games indexed')).toBeTruthy();
+		expect(getByText('2m ago')).toBeTruthy();
+	});
+});
