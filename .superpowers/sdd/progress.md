Task 1: complete (commits e420508..3105de2, review clean after 1 fix round)
Task 2: complete (commits 3105de2..dde1ce6, review clean w/ 1 doc fix)
Task 3: complete (commits dde1ce6..ff2fa2c, review clean)
Task 4: complete (commits ff2fa2c..b858b32, review clean)
Task 5: complete (commits b858b32..d159128, review clean)
Task 6: complete (commits d159128..1024b64, review clean; minor: favicon orphaned, follow-up later)
Task 7: complete (commits 1024b64..8bf9550, review clean/Approved; native `tauri build --debug` verified working after user installed VS C++ Build Tools workload — MSI+NSIS bundles produced successfully)
Task 8: complete (commit 34d69ab; lint/check/test/build all pass; found & fixed 2 real bugs during the sweep: (1) src-tauri/target wasn't ESLint-ignored, causing it to choke on binary build artifacts once a native build populated it; (2) npm run format was never wired to package.json despite prettier being installed — added it plus a .prettierignore that deliberately excludes src/app.css to protect its byte-for-byte drift-free match with tokens.ts, and excludes docs/.serena/.superpowers so historical/tool-owned files aren't reformatted)
Whole-branch review: Approved for merge (Opus reviewer, main..HEAD). 0 Critical, 1 Important (no shared svelte.config.js — vite.config.ts/vitest.config.ts could silently diverge), several Minor (dead adapter-auto dep, placeholder app.test.ts, orphaned favicon.svg, robots.txt cruft, Cargo.toml placeholder metadata, csp:null). Fixed in commit 150aeec: extracted svelte.config.js, removed adapter-auto + app.test.ts. Remaining Minor items deferred to later iterations (favicon/branding, CSP hardening) — not blockers.

---
Iteration 2 (Persistent chrome):
Task 1: complete (commits 1f6b9e2..5edc8c4, review clean/Approved)
Task 2: complete (commits 5edc8c4..1e07c20, review clean/Approved)
Task 3: complete (commits 1e07c20..df4931f, review clean/Approved; minor note: only 3/9 nav entries get full value assertions, matches brief's own test spec, non-blocking)
Task 4: complete (commits df4931f..0195088, review clean/Approved; cargo check passed, 24/24 JS tests)
Task 5: complete (commits 0195088..441b96d, review clean/Approved; note: manual drag-region smoke test recommended in Task 9's dev-server check)
Task 6: complete (commits 441b96d..52f57e8, review clean/Approved)
Task 7: complete (commits 52f57e8..9681bfb, review clean/Approved)
Task 8: complete (commits 9681bfb..a7b22ae, review clean/Approved)
Task 9: complete (commits a7b22ae..2bb99d2, review clean/Approved)
Task 10: complete (final verification sweep: lint/check/test/format/build/native-build all pass)

Whole-branch review: Approved for merge (Opus reviewer, 150aeec..be840af). 0 Critical, 0 Important. 2 Minor: (1) app-state.svelte.ts singleton seeded from defaultState object literal directly rather than a fresh copy — pre-existing Iteration-1 code, not in this diff, flagged only because Iteration 2's test-reset pattern leans on it harder; cheap hardening (`$state(createAppState())`) deferred to next iteration; (2) window-dot left-to-right minimize/maximize/close ordering has no source-of-truth in the design reference — spec-consistent, already flagged as an assumption in Task 5, no action needed. Design fidelity verified verbatim against README §6.1 and nav-items against SecondBoard.dc.html; token/CSS mirror, thin-wrapper rule, capability minimality, single-main landmark, and appState reset pattern all confirmed intact.

---
Iteration 3 (The Board component):
Task 1: complete (commits cc76185..e702eca, review clean/Approved)
Task 2: complete (commits e702eca..9bf450d, review clean/Approved)
Task 3: complete (commits 9bf450d..3ffb830, review clean/Approved; minor: pieces.ts key type not explicitly coupled to PieceColor/PieceType via satisfies, functionally equivalent)
Task 4: complete (commits 3ffb830..a11ded4, review clean/Approved)
Task 5: complete (commits a11ded4..2138301, review clean/Approved)
Task 6: complete (commits 2138301..d107dd3, review clean after 1 fix round: added transitionend cleanup + double-fire-safety test coverage)
Task 7: complete (commits d107dd3..d3cc138, review clean/Approved after 2 guidance rounds: fixed jsdom CSSOM hex-normalization + data-URI-quote-rejection test issues via mocked PIECE_SPRITES + computed rgb assertions; component itself untouched/verbatim)
Task 8: complete (commits d3cc138..d251dc9, review clean/Approved)
Task 9: complete (commits d251dc9..25a50f9, review clean/Approved; adapted a pre-existing page.test.ts case that became unreachable once the harness was wired in; visual pixel-check vs screenshots still needs a human pass)
Task 10: complete (final verification sweep: lint/check/test/format/build/native-build all pass; found & fixed 1 real bug during the sweep: pieces.ts used a bare `?url` suffix for the 12 piece-sprite SVG imports, but since every sprite is under Vite's default 4KB assetsInlineLimit, `?url` alone still let Vite inline them as base64 data URIs in the JS bundle instead of emitting the hashed static asset files the code comment and Task 3's design intent both describe — switched to `?url&no-inline` (a documented Vite suffix, ambient-typed in vite/client.d.ts) so the build now emits the expected 12 hashed SVGs under build/_app/immutable/assets/, verified present after a clean rebuild and again via the native tauri build's frontend step; `npm run format` also reformatted ~8 files that had never been run through the current Prettier config (long array/arg-list line-wrapping only, no semantic changes) — reviewed each diff, all cosmetic)
Task 10: complete (commits 25a50f9..a22693a, review clean/Approved; found & fixed 1 real issue during sweep: pieces.ts ?url imports were silently base64-inlined by Vite for sub-4KB SVGs instead of emitted as hashed files, contradicting the "offline bundled hashed asset" doc comment and this task's own build-output check — fixed via ?url&no-inline suffix, confirmed valid against installed Vite's client.d.ts; functionally the base64 form would have worked identically at runtime, so this was a spec-conformance/doc-accuracy fix rather than a runtime bug)
Whole-branch review: Approved for merge (Opus reviewer, 40a2a99..a22693a). 0 Critical, 0 Important. Minor (deferred): FILES const duplicated across geometry.ts/build-squares.ts/dev-fixtures.ts (dev-fixtures.ts slated for deletion anyway); capturedInfo/evalBarPct/center exported but unused this iteration (intentional pre-port for Iteration 4 eval bar). Tracked forward item: dev-fixtures.ts (mock SAN engine) currently ships in the dev bundle via +page.svelte import — Iteration 4 MUST delete/replace this per LOGIC.md's "must not ship" warning; correctly isolated from board/index.ts public API in the meantime. Fidelity spot-checks (square colors, badge box-shadow, bpulse two-layer keyframes, arrow geometry, coord-label colors) all verified verbatim against reference/SecondBoard.dc.html and view-math.js.

---
Iteration 4 (Game Review screen):
Task 1: complete (commits c54543e..2409089, review clean after 1 fix round: corrected yOf truncation->rounding and dots-loop i-1->i indexing, both verified against design_handoff_secondboard/reference/logic/view-math.js since the plan's own Step 2 test fixture had wrong expected values; minor: harmless .toFixed(1) string-padding cosmetic divergence from reference, inert for SVG rendering)
Task 2: complete (commits 2409089..d9fb1b4, review clean/Approved; ground-truth cross-check against data.js/chess-mock.js confirmed byte-for-byte match, no corrections needed; minor: components/README.md still references deleted dev-fixtures.ts path, deferred)
Task 3: complete (commits d9fb1b4..cb0f94e, review clean/Approved; getReviewPly/getPlayerRows verified line-by-line against SecondBoard.dc.html renderVals() 1221-1330, ply-0 classCode:null confirmed correct vs reference's internal 'book' placeholder which is always gated out)

STOPPED HERE by user request (usage limit) after Task 3 review approval. NEXT: Task 4 (appState screen/tab/ply transitions). To resume: read this file, confirm HEAD matches cb0f94e-derived history via git log, then continue subagent-driven-development from Task 4 using task-brief script on docs/superpowers/plans/2026-07-15-iteration-04-game-review.md.
Task 4: complete (commits f149d9e..99b165b, review clean/Approved)
Task 5: complete (commits d2237b7..2643e3b, review clean/Approved; minor doc note: brief's Interfaces section lists TOKENS.review.moveTint as consumed but component doesn't use it — that's a separate move-list-row concern, not a badge defect)
Task 6: complete (commits 779137c..d5af265, review clean/Approved; plan-mandated conflict noted and resolved by user: label colors #20222E/#E3E6EE hardcoded per brief's own code with no TOKENS backing — accepted as-is, not worth a follow-up token for two single-use colors)
Task 7: complete (commits 18cbcc7..52dc696, review clean/Approved; test assertion adjusted for jsdom style.cssText re-serialization spacing (border-radius:8px -> border-radius: 8px), verified independently by reviewer, component itself untouched/verbatim)
Task 8: complete (commits 150e717..245f93c, review clean/Approved with 1 plan-mandated finding: .new-game button hardcoded border/text hex despite matching TOKENS.review.newGameBorder/newGameText already existing; user chose to fix now -> commit 7ffacda swaps in an inline newGameStyle derived from TOKENS.review.newGameBg/Border/Text, mirroring the existing avatarStyle/clockStyle pattern; test 4/4 pass and pnpm run check shows only pre-existing unrelated errors after the fix; cleanup() added in PlayerRow.test.ts's 4th case for testing-library render-leakage, verified test-only)
Task 9: complete (commits baf84d6..33dbc2d, review clean/Approved; component verbatim per brief; 2 brief test-fixture bugs found & fixed test-only (getByText('is') ambiguous substring match -> scoped to .word; missing cleanup() between renders -> added, matching PlayerRow.test.ts precedent); minor deferred: best-strip icon/bg hardcodes #4ADEA0/#0d0f16/rgba(74,222,160,.25) verbatim from brief, no TOKENS entries — flagged for final review triage, not fixed now)
Task 10: complete (commits 4dc2cb7..349015e, review clean/Approved; verbatim per brief, 31-entry SAN_LIST/CLASS_CODES wiring verified correct including odd-count final-row white-only rendering and manual scrollTop-delta auto-scroll (not scrollIntoView); minor deferred: unused `tick` import and no rAF-cancellation cleanup in the scroll $effect, both inherited verbatim from brief, no functional risk)
Task 11: complete (commits 47f89d4..076eadb, review clean/Approved; BreakdownTable/PhaseTable/AccuracyBlock all verbatim per brief, AccuracyBlock's self-corrected inline-style avatar backgrounds (TOKENS.review.avatarWhiteBg/avatarBlackBg) applied correctly, grid-placement distinction between BreakdownTable's dedicated badge column vs PhaseTable's 2nd/4th-column placement verified; minor deferred: a few pre-brief hardcoded literals (#c2c7d4, #181a24) and repeated grid-template-columns string across 3 files, not in scope of this task's token-reuse note)
Task 12: complete (commits 56ae581..a27f882, review clean/Approved; component verbatim, classCode??'book' fallback correctly placed here (not in CoachCard); real cross-component .row class collision found between already-approved CoachCard.svelte and MoveList.svelte when rendered as siblings -- resolved by scoping the test query to MoveList's [data-sb-movelist="1"] root rather than touching either approved component, verified by reviewer as a real, non-vacuous fix)
Task 13: complete (commits 944fccf..a2260ec, review clean/Approved; ReviewTab verbatim per brief, EvalGraph/AccuracyBlock/BreakdownTable/PhaseTable composition props verified correct)
Task 14: complete (commits f1b305c..d2930c5, review clean/Approved; DetailsTab/ExploreTab verbatim per brief, zero/one-prop surfaces correct, Icon usage type-checked against real Icon.svelte interface; minor note: static mock content (win rate, date, etc.) still needs wiring to real derived data in a later iteration, expected per this task's scope)
Task 15: complete (commits 6c38757..ab48954, review clean/Approved; NavControls/BottomBar verbatim per brief, intentional dual-onNext wiring and BottomBar's height=62 (vs Review tab's 66) both verified correct)
Task 16: complete (commits 283d058..53d118e, review clean/Approved; ReviewPanel verbatim per brief, all 4 tab bodies/BottomBar-review-exclusion/direct appState.tab mutation/flip-btn-only wiring/no-duplicated-keyboard-handling all verified correct against real app-state.svelte.ts exports and Sidebar.svelte's precedent convention; minor deferred: inert search icon-btn with no onclick, matches brief verbatim)
Task 17: complete (commits f08d532..2ebaa7c, review clean/Approved; GameReviewScreen verbatim per brief, onMount window keydown attach/cleanup, PlayerRow top/bottom swap via getPlayerRows, EvalBar whiteAtBottom derivation, Board's 6 props, and ReviewPanel's onToggleFlip/onOpenOpenings all verified against real downstream Props interfaces; keydown test confirmed to exercise the real window listener not a mock)
Task 18: complete (commits 5f40d69..28f878a, review clean/Approved; OnboardingScreen verbatim per brief, pgnText cosmetic-only wiring, inert Upload button, and no-props screen convention all verified against real app-state.svelte.ts startReview() behavior; minor deferred: placeholder string duplicates a truncated version of SAMPLE_PGN, both verbatim from brief, could drift later)
Task 19: complete (commits a6b55fc..f130374, review clean/Approved; +page.svelte harness fully removed and replaced with real OnboardingScreen/GameReviewScreen wiring, page.test.ts updated to assert real behavior (not old placeholder strings), README.md/charts/index.ts updated per brief; out-of-brief-scope fix included and verified legitimate: widened PlayerRowData.captured to PieceColor/PieceType in review.ts, fixing a pre-existing (Task 4) type error that only surfaced once the dangling dev-fixtures import was removed -- confirmed no runtime behavior change (kings can't be captured) and no downstream break (PieceSpriteKey already covers K sprites); full suite 163/163 pass, pnpm run check 0 errors, pnpm run build succeeds.
ALL 19 TASKS COMPLETE. Next: final whole-branch review (Opus/most-capable model) per subagent-driven-development skill, then superpowers:finishing-a-development-branch.
Whole-branch review: Approved for merge (Opus reviewer, c54543e..31258c2). 0 Critical, 0 Important. 6 Minor findings: (1) stale README "Still to come" line listing already-landed EvalBar/CoachCard/MoveList -- FIXED in commit 7ef6a34; (2) review-color hardcoded hex cluster across EvalBar/CoachCard/AccuracyBlock duplicating existing TOKENS.review values in scoped CSS (JS-only token design can't be referenced from <style> without a CSS var) -- deferred, recommend a follow-up to mirror ~4 reused colors into app.css CSS vars; (3) grid-template-columns "88px 1fr 36px 1fr" duplicated 4x across 3 files -- deferred, same root cause as (2); (4) accumulating a11y warnings (icon-only nav buttons, clickable non-interactive elements) all verbatim from design reference -- deferred to a later dedicated a11y pass; (5) MoveList's unused `tick` import -- trivial, deferred; (6) OnboardingScreen placeholder duplicates truncated SAMPLE_PGN -- deferred, cosmetic drift risk only. Reviewer independently verified both flagged risks as safe: the CoachCard/MoveList .row collision has zero runtime impact (Svelte scopes styles per-component via hash suffix), and the Task 19 PlayerRowData.captured type widening is sound (kings structurally can't be captured, no downstream break). Full suite 163/163, check 0 errors, build succeeds, confirmed independently by the reviewer at HEAD.
BRANCH READY TO MERGE. Next: use superpowers:finishing-a-development-branch.

---
Iteration 5 (Rust analyze_fen + Stockfish UCI):
Task 1: complete (commit 5e3de9d, review clean/Approved; UCI info/bestmove line parsers, 7/7 tests pass, no new deps, no non-test unwraps; minor note: parse_info_line/parse_bestmove_line are pub(crate) but unused until Task 2 wires them in -- expected task-splitting, not a defect)
Task 2: complete (commit c71dec8, review clean/Approved; real Stockfish 18 process driven over UCI, 9/9 tests pass including a genuine real-engine analysis test independently re-verified by the reviewer; minor notes deferred: mate distance collapsed to flat +/-100000 magnitude with no ply-count field, sequential stdin-write/stdout-read has a latent (currently non-triggered) pipe-buffer deadlock risk if Stockfish ever emits more info output than the OS pipe buffer before reads begin)
Task 3: complete (commit ff0a4ce, review clean/Approved; analyze_fen Tauri command registered, camelCase serde boundary verified, async+spawn_blocking pattern correct, 10/10 rust tests pass, cargo check 0 warnings)
Task 4: complete (commit ee7ff5b, review clean/Approved; FEN serialization + minimal SAN helpers, 7/7 tests pass, reviewer hand-verified FEN rank encoding and all 3 SAN cases correct; minor note: Position type isn't Partial so piece-lookup truthiness checks are runtime-only safety nets, pre-existing type looseness not introduced by this task)
Task 5: complete (commit 924bddc, review clean/Approved; analyzeFen invoke wrapper matches window.ts convention exactly, camelCase interface verified, index.ts re-exports additive)
Task 6: complete (commits 02cadd4..c900eb5, review clean/Approved after 1 fix round; real per-ply analysis orchestration with Promise.all fan-out, bestMoves[ply+1] mapping and final-position exclusion verified correct; found & fixed 1 real bug (Important, my own plan's sample code was at fault): toWhitePovEval hardcoded +100 magnitude for any mate score regardless of evalCp's actual sign, inverting evals for positions where the mover is being mated -- fixed by dropping the isMate special-case entirely and using evalCp/100 uniformly since Rust already signs mate scores +/-100000 relative to the mover; strengthened test to cover both favorable and unfavorable mate cases, reviewer hand-verified the new test would have caught the original bug; minor deferred: promotion UCI strings silently truncated (no promotions in this mock game), malformed bestMoveUci silently skipped with no logging)
Task 7: complete (commit 21df961, review clean/Approved; appState gains evalPerPly/bestMoves/analysisStatus seeded from fresh mock-array copies, startReview wired to refreshRealAnalysis with idle->loading->ready/error transitions, no unhandled-rejection risk verified, no breakage to Sidebar.test.ts/page.test.ts's createAppState()/Object.assign reset pattern; 10/10 focused + 179/179 full suite pass; minor deferred: new 'real analysis loading' describe block has no beforeEach resetting the 3 new fields (order-dependent but currently safe, brief-mandated code), createAppState()'s shallow spread now shares array/object references across calls (pre-existing helper, not exercised by any current test))
Task 8: complete (commit b7bdcf1, review clean/Approved; getReviewPly backward-compatible default-parameter extension verified against all 8 pre-existing calls, GameReviewScreen/AnalysisTab/ReviewTab/BottomBar/ReviewPanel all correctly wired to appState.evalPerPly/bestMoves, Analyzing... indicator correctly gated and reuses existing --color-text-tertiary token; 181/181 full suite pass; minor deferred: pre-existing unrelated hardcoded hex in ReviewPanel.svelte:232 noted but out of scope)
Task 9: complete (commit 7844497, review clean/Approved; full verification sweep -- 181/181 JS tests, pnpm run check 0 errors/15 pre-existing warnings, cargo test 10/10 (real Stockfish 18 genuinely exercised, confirmed via --nocapture), cargo check 0 warnings, pnpm run build succeeds; found & fixed 4 real lint errors: eslint.config.js had a Svelte-file config gap where base no-unused-vars false-flagged TS function-type Props params, plus a genuinely unused `tick` import in MoveList.svelte and a useless-mustache in OnboardingScreen.svelte; manual GUI dev-server smoke test (brief Step 5) explicitly SKIPPED -- headless environment has no display server, cannot launch a native Tauri window; flagged for the user to verify manually with a real display before shipping)
ALL 9 TASKS COMPLETE. Next: final whole-branch review (most-capable model) per subagent-driven-development skill, then superpowers:finishing-a-development-branch.
Whole-branch review: Approved for merge (Opus reviewer, aff0cd0..3abe1e5, 23 commits). 0 Critical, 1 Important found & fixed: loadRealAnalysis() fired all 32 analyzeFen calls in one unbounded Promise.all, meaning up to 32 concurrent real Stockfish processes (256MB hash + cores-1 threads each, ~8GB peak) -- risk of OOM/thrashing on hardware weaker than the dev machine, invisible to automated tests since they mock analyzeFen entirely. Fixed in commit 3abe1e5: bounded-concurrency batching (batch size 4) via a new mapWithConcurrency helper, reviewer independently hand-verified the positional-write ordering logic is correct (not resolution-order dependent) rather than trusting the fixer's report. Minor deferred (all acceptable Phase-0 spike simplifications, no action needed): no FEN-injection sanitization on the public analyze_fen command (not exploitable today, FENs are internally generated), no UI surface for analysisStatus==='error', real eval line renders alongside mock classification dots/coach text (inherent to the scoped classification boundary), moveToSan would mislabel a castling/promotion best-move suggestion if the real engine ever suggested one, isMate/pv fields deserialized but unused, new concurrency test's ordering assertion is weaker than ideal (uniform-delay mock can't distinguish resolution-order from positional-order bugs, though the shipped code is correct by construction). Also includes 2 small out-of-plan fixes: vite.config.ts strictPort (prevents Tauri's hardcoded devUrl from silently loading an unrelated project's dev server when port 5173 is taken) and a .gitignore Sonarlint entry -- both confirmed minimal/isolated. Full suite 182/182 pass (JS), Rust 10/10 pass with a genuinely-exercised real Stockfish 18 binary, pnpm run check 0 errors, cargo check 0 warnings, pnpm run build succeeds.
BRANCH READY TO MERGE. Outstanding: manual GUI dev-server smoke test (Task 9 Step 5) could not be run in this headless sandbox -- user should verify with a real display (WEBKIT_DISABLE_DMABUF_RENDERER=1 pnpm tauri dev) before fully trusting the real analysis path end-to-end. Next: use superpowers:finishing-a-development-branch.

---
Post-review manual testing finding (discovered by user via native Tauri app + WebKit inspector network trace):
User reported the "Analyzing with Stockfish..." indicator never visibly appeared during real testing, with some input lag on the first few moves after starting a review. Investigated via a WebKit Timeline recording (logs/gamereview.json) the user captured: confirmed all 32 analyze_fen IPC calls succeeded (200), correctly batched in groups of 4 per the whole-branch review's concurrency fix, running for ~19 real seconds total. Root cause: on the user's 12-core machine, each Stockfish instance was configured with Threads=cores-1=11; with 4 instances running concurrently (the batch fix), that's 44 threads oversubscribing 12 cores, likely starving the WebKit renderer's own thread badly enough to drop the frame(s) where the transient loading note would have painted, plus causing the observed input lag. Fixed in commit f8d296e: Threads is now divided by a CONCURRENT_ENGINE_INSTANCES=4 constant (kept in sync with engine-analysis.ts's batch size), keeping total engine threads bounded to roughly cores-1 in aggregate rather than per-instance. Verified: cargo test 10/10 pass, cargo check 0 warnings. Pushed to PR #4 (fast-forward, no history rewrite needed this time).

---
Iteration 6 (PGN parse via shakmaty + move navigation):
Task 1: complete (commit 4a32f67, review clean/Approved after 1 fix round; real shakmaty/pgn-reader PGN parser, 8/8 tests pass; found & fixed 2 real bugs in the plan's own test fixtures during implementation, both independently verified by the controller before dispatching the fix: (1) plan wrongly asserted 16.Ne5 originates from f6 -- correct chess tracing shows it's actually f3 (the knight that's been there since move 3; the f6 knight from 8...Nf6 never moves again), (2) plan's illegal-move test used Qxf9 which pgn-reader's tokenizer silently truncates before ever reaching our error path (rank 9 isn't a valid square) -- replaced with a syntactically-valid-but-chess-illegal Kd8 (moving onto Black's own un-moved queen); also had to bump shakmaty from the plan's stated 0.27.3 to 0.28.0 since pgn-reader 0.27.0 transitively requires 0.28.0 and having both in the dependency graph caused real type conflicts -- confirmed via cargo tree, single resolved version now; minor deferred: move_dto's Move::from().unwrap_or_else(||to()) fallback for Put-drop moves is unreachable via orthodox PGN today but silently sets from==to rather than erroring if it ever were)
Task 2: complete (commit 440e3e6, review clean/Approved; parse_pgn Tauri command registered alongside analyze_fen, correctly left non-async since PGN parsing is in-process CPU work not a process spawn, 20/20 rust tests pass; illegal-move test correctly used the Task-1-verified Kd8 substitution instead of the plan's broken Qxf9 literal)
Task 3: complete (commit 5401883, review clean/Approved; parsePgn invoke wrapper matches window.ts/engine.ts convention exactly, camelCase interface verified, index.ts re-export additive)
Task 4: complete (commit 751ad54, review clean/Approved after 1 fix round; real async startReview wired to parsePgn with success/failure paths, GameData defined locally in appState (Task 5 will relocate), getMaxPly plain-function conversion complete (zero remaining MAX_PLY code refs), onboarding error banner reuses --color-accent-red token; found & fixed a real plan gap: making startReview async broke 3 pre-existing sync-calling tests from iterations 4/5 (verified genuine, not implementer error) -- fixed by awaiting startReview in those tests with inline mock setup; implementer also proactively flagged (correctly, without fixing) that OnboardingScreen.test.ts and GameReviewScreen.test.ts are now broken by the same async-timing change -- tracked forward for Task 5 (GameReviewScreen.test.ts, already in scope there) and Task 7's final sweep (OnboardingScreen.test.ts))
Task 5: complete (commit bbd25e7, review clean/Approved; GameData relocated to review.ts with exact shape, getReviewPly's isSample gating hand-traced correct for both true/false cases by the reviewer (coachMove stays real regardless of isSample, only classCode/best/coachText gated), app-state.svelte.ts reconciled to import GameData from review.ts (local duplicate removed), GameReviewScreen/AnalysisTab call sites updated; found & fixed 2 out-of-brief collateral test breakages (ReviewPanel.test.ts, src/routes/page.test.ts) -- both direct mechanical consequences of getReviewPly/getPlayerRows requiring non-null game, fixed with the same minimal mock-data-sourced fixture pattern reused across all 4 touched test files; full suite 186/187, 1 expected/deferred failure in OnboardingScreen.test.ts tracked for Task 7; minor deferred: identical GameData fixture literal duplicated across 4 test files, no shared test helper -- stylistic only)
Task 6: complete (commits aaa4630..62a7313, review clean after 1 fix round; found & fixed a real spec deviation the implementer's own fix pass had introduced: mock-data.ts still exported SAN_LIST after the mock-engine-resurrection fix, contradicting brief Step 5 -- fixed by inlining the same 31-move fixture locally into the 4 consuming test files (mirroring MoveList.test.ts's existing precedent) and removing the export; reviewer independently verified mock-engine deletion, zero `as any` in src/lib/game|components|stores, and the pgn.ts ParsedGame.positions/moves type-tightening (2f9d2fa) as a legitimate minimal fix, not scope creep; minor deferred: src/lib/components/README.md still documents mock-engine.ts as live, stale doc reference)
Final verification sweep: complete (JS/TS suite 46/46 files, 186/186 tests pass after fixing OnboardingScreen.test.ts's pre-flagged async-startReview mock gap (Task 4/5 tracked-forward item; test-only fix, mirrors app-state.test.ts's existing parsePgn/loadRealAnalysis mock pattern); pnpm run check 0 errors/15 pre-existing warnings; pnpm run lint 0 errors; cargo test 20/20 pass including Task 1's 8 pgn-module tests and Iteration 5's real-Stockfish integration test -- the latter was silently short-circuiting in this sandbox for lack of a stockfish binary on PATH (a genuine environment gap, not a code bug) until Stockfish 18 was installed via winget and PATH-linked, after which the suite took 5.53s instead of 0.02s, confirming the real engine was genuinely exercised; cargo check 0 warnings; pnpm run build succeeds; manual GUI PGN-paste smoke test (Step 5) could not be run in this headless sandbox -- flagged for the user to verify with a real display before merging)
Task 7: complete (commit 20c5802, review clean/Approved; JS 186/186, Rust 20/20 (real Stockfish genuinely exercised), check/lint/build all clean; fixed a real test gap: OnboardingScreen.test.ts wasn't updated for Task 4's async startReview(); Step 5 GUI smoke test explicitly deferred to a human with a real display per documented headless-sandbox limitation)
ALL 7 TASKS COMPLETE. Whole-branch review: Approved for merge (Opus reviewer, main..20c5802, 20 commits). 0 Critical, 2 Important found & fixed in commit 39aa313: (1) AnalysisTab.svelte's `data.classCode ?? 'book'` silently defeated the isSample gating built in Task 5/6, showing a misleading "book move" badge on every ply of a real pasted game -- fixed by widening CoachCard's classCode prop to ClassCode|null and rendering a genuinely neutral state (existing neutral tokens, no new TOKENS.classification entry) when null; (2) app-state.svelte.ts's startReview() catch block only handled `err instanceof Error`, but Tauri v2 rejects Result<T,String> failures with a plain string, so real Rust parse-error messages were always discarded for the generic "Failed to parse PGN." fallback -- fixed to check typeof err === 'string' first. Both fixes independently re-verified by a second reviewer pass (commit 39aa313 reviewed clean, 188/188 tests, real regression coverage added for both). Minor deferred (not blockers): stale src/lib/components/README.md mock-engine.ts reference; real Stockfish bestMoves computed but never surfaced for non-sample games (consistent with classification-deferral scope); BreakdownTable/PhaseTable/player rows still always show sample mock data regardless of isSample (documented DetailsTab deferral, not a regression); pgn.rs Put-drop from()==to() fallback (unreachable via orthodox PGN).
BRANCH READY TO MERGE. Outstanding: manual GUI PGN-paste smoke test (Task 7 Step 5) could not be run in the headless sandbox -- user should verify with a real display before fully trusting the parse/navigate/error-banner paths end-to-end. Separately: user's local Stockfish binary was installed but not on PATH -- added C:\Program Files\stockfish to user PATH via setx; requires restarting terminal/app to take effect. Next: use superpowers:finishing-a-development-branch.

---
Iteration 7 (Title bar native controls + real winner/accuracy):
Plan: docs/superpowers/plans/2026-07-17-titlebar-and-real-accuracy.md
Branch: fix/titlebar-and-real-accuracy, base fa182e5
Task 1: complete (commit fa182e5..17667f0, review clean/Approved; minor deferred: no test for [Result "*"] placeholder, decode_known_tag handles it generically per existing pattern)
Task 2: complete (commits 17667f0..823c237, review clean/Approved; fixture sweep independently verified exhaustive across 5 GameData literal sites, no fixes needed)
Task 3: complete (commits 823c237..cb5ccea, review clean/Approved; formula constants and mover-POV sign convention independently hand-verified for both colors by the reviewer; minor deferred: single-side zero-moves null path untested)
Task 4: complete (commits cb5ccea..b752be4, review clean/Approved; fallback pattern verified byte-for-byte matching getPlayerRows, Task 3 import signatures verified correct; minor deferred: no test asserts the numeric .toFixed(1) accuracy branch directly, plan-mandated gap not counted against compliance)
Task 5: complete (commits b752be4..6f6bcc2, review clean/Approved; winner tinting confirmed genuinely dynamic on both columns via markup trace + behavioral test, AccuracySide shape verified matching Task 4's export, CSS/Game Rating untouched)
Task 6: complete (commits 6f6bcc2..ed77799, review clean/Approved; appState.game! direct-import pattern independently confirmed identical to AnalysisTab.svelte's precedent, evalPerPly prop reuse confirmed, ReviewPanel.svelte confirmed untouched)
Task 7: complete (commits ed77799..16564fb, review clean/Approved; genuine DOM trailing-reorder confirmed not a CSS trick, app-region:no-drag confirmed preserved, close-hover currentColor propagation confirmed, all dead status-pill CSS removed; minor note: aria-label text is "Minimize window" etc per brief's own reference code, not bare "Minimize" -- non-issue, tests use getByTitle)
Task 8: automated verification complete (controller-run, not subagent-dispatched): full JS suite 221/221 pass (47/47 files), Rust 29/29 pass (real Stockfish genuinely exercised via engine::analyze_tests::analyzes_the_starting_position_with_a_real_stockfish), pnpm check 0 errors/14 pre-existing warnings (app-region CSS-property warnings, same category pre-dating this branch), pnpm lint clean. Manual GUI smoke test (pnpm exec tauri dev, visual confirmation of title bar + accuracy block) deferred to the user -- this sandbox is headless.
ALL 7 CODE TASKS COMPLETE. Next: final whole-branch review (most-capable model) per subagent-driven-development skill.
Whole-branch review: Approved for merge with fixes (Opus reviewer, fa182e5..16564fb, 7 commits). 0 Critical, 1 Important found & fixed in commit 9a528a0: ReviewTab.svelte computed accuracy from the raw evalPerPly prop, but startReview() seeds appState.evalPerPly to a full-length zero-array placeholder before real Stockfish analysis completes -- computeGameAccuracy's length<2 guard didn't catch a full-length zero array, so both sides rendered a fabricated 100.0/100.0 accuracy during the loading window and permanently on analysis error, reintroducing exactly the fabricated-number defect this branch existed to remove. Fixed by gating on appState.analysisStatus === 'ready' (allowlist, not denylist) before passing evalPerPly into getAccuracySummary, defaulting to [] (-> null -> "-") for idle/loading/error alike. Fix independently re-reviewed and confirmed to close the gap for all analysisStatus values, not just 'loading'; regression test verified to genuinely reproduce the bug pre-fix. Minor deferred (non-blocking): duplicated result-string mapping between formatResultLabel and resolveWinner; redundant `?? null` in app-state.svelte.ts (ParsedGame.result already string|null); plan's own comment inaccurately claims decode_known_tag maps "*" to None for Result (it doesn't -- only ""/"?" -- but UI handles Some("*") correctly downstream via resolveWinner/formatResultLabel regardless, so no user-visible bug, just a plan-doc inaccuracy); no positive-path test proving a real evalPerPly renders a non-"-" accuracy when analysisStatus==='ready'. Full JS suite 222/222 (47 files), Rust 29/29 (real Stockfish exercised), pnpm check 0 errors/14 pre-existing warnings, pnpm lint clean.
BRANCH READY TO MERGE. Outstanding: manual GUI smoke test (title bar top-right native controls + Local/Offline pill removed + real accuracy/winner highlighting) could not be run in this headless sandbox -- user should verify with pnpm exec tauri dev before merging. Next: use superpowers:finishing-a-development-branch.

---
Iteration 8 (Move classification - deterministic EP-cutoff core):
Plan: docs/superpowers/plans/2026-07-20-iteration-07-move-classification.md
Branch: feat/reproduce-chesscom, base 135e86c
Task 1: complete (commit 135e86c..d2407c5, review clean/Approved; classify.ts pure EP-cutoff classifier, exact cutoff table + mover-POV win% attribution verified against accuracy.ts/notation.ts, 9/9 tests pass, not yet wired to any caller (expected, Task 2+))
Task 2: complete (commits d2407c5..220df07, review clean/Approved; appState.classCodes wired via classifyGame in refreshRealAnalysis, reset to [] in startReview, untouched on error; 18/18 tests pass including loading-window and rejection edge cases)
Task 3: complete (commits 220df07..842fc31, review clean/Approved; getReviewPly's isSample gate removed, real classCodes 5th param w/ mock CLASS_CODES default preserving un-migrated callers, UNCLASSIFIED_COACH_TEXT copy updated, doc comment self-corrected; 20/20 tests pass)
Task 4: complete (commits 842fc31..199a6fc, review clean/Approved; MoveList's isSample prop replaced with classCodes: ClassCode[], badge gating now per-ply-entry-based, AnalysisTab wires appState.classCodes into both getReviewPly and MoveList; single-caller risk verified clean; 11/11 focused tests pass, full suite independently re-verified 48 files/242 tests all green after an implementer-reported false-alarm flake was ruled out)
Task 5: complete (commit 199a6fc..e2eb167, review clean/Approved; GameReviewScreen's getReviewPly call gains appState.classCodes 5th arg for board arrow, 1-line diff verified exactly scoped; 4/4 tests pass)
Task 6: complete (commits e2eb167..caaed74, review clean/Approved; ReviewTab/BottomBar's CLASS_CODES mock imports replaced with classCodes prop, ReviewPanel threads appState.classCodes to both, accuracy/analysisStatus gating in ReviewTab confirmed byte-identical/untouched; 8/8 focused + 242/242 full suite pass)
ALL 6 CODE TASKS COMPLETE. Next: Task 7 (full verification pass).
Task 7: automated verification complete (controller-run, not subagent-dispatched): full JS suite 48/48 files, 242/242 tests pass; pnpm check 0 errors/14 pre-existing warnings (same categories as prior iterations: a11y, app-region, state_referenced_locally); pnpm lint clean; pnpm build succeeds; grep confirms CLASS_CODES only remains in mock-data.ts (export)/mock-data.test.ts (own unit test)/classify.ts+review.test.ts (doc/comment references)/review.ts (intentional default-parameter backward-compat fallback) -- no stray direct imports left anywhere. Manual GUI smoke test (pnpm exec tauri dev) deferred to the user -- this sandbox is headless, consistent with every prior iteration.
ALL 7 TASKS COMPLETE. Next: final whole-branch review (most-capable model) per subagent-driven-development skill.
Whole-branch review: Approved for merge (Opus reviewer, 135e86c..caaed74, 6 commits). 0 Critical, 0 Important. 3 Minor (all non-blocking): (1) classify.ts's exact-zero-loss "Best" cutoff means real floating-point eval drift skews the distribution toward "Excellent" vs chess.com's own feel -- reviewer confirmed this is correct-to-spec per the plan's Global Constraints (Best = 0 exactly), a plan-level fidelity tradeoff not an implementation bug; flagged for awareness during the Task 7 visual check, not a fix item; (2) review.ts's classCodes=CLASS_CODES default parameter is a latent footgun if a future production caller forgets to pass appState.classCodes (no type error would catch it) -- both current production callers (AnalysisTab, GameReviewScreen) do pass it correctly; suggested a follow-up comment, non-blocking; (3) ReviewTab.svelte gates classCodes on analysisStatus==='ready' implicitly (via appState.classCodes itself) but passes evalPerPly to EvalGraph ungated -- harmless/consistent (flat line + no dots during loading), noted only as an asymmetry vs the accuracy block's explicit gate. Reviewer independently traced the full data flow end-to-end (appState.classCodes -> getReviewPly/MoveList/GameReviewScreen board arrow/ReviewTab+BottomBar eval graphs via ReviewPanel), confirmed POV math correct across the color boundary via worked example, confirmed no consumer still reads mocked CLASS_CODES directly, and confirmed the "fabricated placeholder data" bug class (the exact defect a prior iteration had to fix for accuracy) does not recur here.
BRANCH READY TO MERGE. Outstanding: manual GUI visual smoke test (real move-classification badges/eval-graph dots on both the sample game and a freshly pasted non-sample game, confirming no premature/fake badges during the loading window) could not be run in this headless sandbox -- user should verify with pnpm exec tauri dev before merging. Next: use superpowers:finishing-a-development-branch.

---
Iteration 9 (WDL-based expected score):
Plan: docs/superpowers/plans/2026-07-20-iteration-08-wdl-expected-score.md
Branch: feat/wdl-based-x-score, base a9137aa (merge commit for PR #7)
Task 1: complete (commits a9137aa..22de26b, review clean/Approved after 1 fix round; UCI_ShowWDL enabled + wdl w/d/l parsed into InfoLine/EngineAnalysis, purely additive to engine.rs (57 insertions, 0 deletions), all cp/mate parsing paths untouched; found & fixed a real plan defect during implementation (independently verified by the controller against real Stockfish 18 output before dispatching the fix): the plan's own real-engine test asserted w>100 && l>100 assuming a roughly-balanced startpos WDL, but real Stockfish 18 at depth10/movetime1000 genuinely reports a heavily draw-skewed w=76 d=918 l=6 (correct engine behavior, not a bug) -- loosened to a degenerate-value check (w<1000 && d<1000 && l<1000) that still catches a stub/hardcoded triple while accepting legitimate lopsided real output; 18/18 engine tests + 33/33 full Rust suite pass; minor deferred: found_score_or_pv set asymmetrically (only on full-triple success) vs cp/mate arms (set even on solo parse failure) -- harmless, not a real Stockfish output shape in practice)
Task 2: complete (commits 22de26b..9a76d23, review clean/Approved; AnalyzeFenResult.wdl threaded through the Tauri boundary via the existing From impl, no special serde handling needed for the Option tuple field, purely additive to lib.rs; 34/34 Rust tests pass)
Task 3: complete (commit 9a76d23..13b7b73, review clean/Approved; AnalyzeFenResult.wdl: [number,number,number]|null added (correct mutable-tuple shape matching Tauri's JSON array), both tuple-present and null branches tested; minor deferred: no explicit undefined-key test, non-issue since field is non-optional)
Task 4: complete (commit 13b7b73..a87411d, review clean/Approved; Wdl type + winPercentFromWdl + winPercentForPly (single WDL-vs-sigmoid decision point) added, computeGameAccuracy's new wdlPerPly param verified optional with byte-identical fallback both by trace and by confirming the un-migrated review.ts call site; no-regression test locks in pre-existing exact numeric value; 23/23 accuracy tests + 251/251 full suite pass; minor deferred: truthy-check on wdl tuple vs explicit != null, non-issue given w+d+l=1000 invariant)
Task 5: complete (commit a87411d..32e1a1c, review clean/Approved; classifyGame delegates to Task 4's winPercentForPly (verified no duplication), optional wdlPerPly param confirmed byte-identical fallback for the un-migrated app-state.svelte.ts call site, header doc comment updated in sync; 11/11 tests pass)
Task 6: complete (commit 32e1a1c..ce0a8f0, review clean/Approved; toWhitePovWdl (win/loss swap, draw untouched) added alongside toWhitePovEval, flip direction independently hand-verified correct via worked example against the brief's own test value, wdlPerPly index-alignment with evalPerPly structurally guaranteed by identical map construction, undefined-vs-null ternary equivalence confirmed so all pre-existing tests stay green unmodified; 8/8 focused + 255/255 full suite pass; reviewer's one ⚠️ (PieceColor's exact union) resolved by the controller -- already confirmed earlier this session via grep: `export type PieceColor = 'w' | 'b';`, no third variant; minor deferred: inline 'w'|'b' literal vs PieceColor type used inconsistently between the two adjacent toWhitePov* helpers, cosmetic only)
Task 7: complete (commit ce0a8f0..1aafabe, review clean/Approved; appState.wdlPerPly wired via startReview/refreshRealAnalysis mirroring the established classCodes pattern, getAccuracySummary/ReviewTab/ReviewPanel threaded with identical evalPerPly-parallel gating (confirmed genuinely parallel, not just similarly-named); pre-existing test-mock undefined-vs-[] gap confirmed harmless by tracing winPercentForPly's optional-chaining fallback; 46/46 focused + 258/258 full suite pass, pnpm check 0 errors)
ALL 7 CODE TASKS COMPLETE. Next: Task 8 (full verification pass).
Task 8: automated verification complete (controller-run, not subagent-dispatched): cargo test 34/34 pass; full JS suite 48/48 files, 258/258 tests pass; pnpm check 0 errors/14 pre-existing warnings (same categories as prior iterations); pnpm build succeeds. Found & fixed 1 real lint error during the sweep (commit 010083f): Task 6's WDL White-POV-flip test had an unused `fen: string` mock-callback param (plan-authored defect, verbatim from the plan text, not an implementer error) tripping @typescript-eslint/no-unused-vars -- removed the unused param, re-verified lint clean + the affected test file (8/8) + full suite (258/258) + build all still pass. Manual GUI smoke test deferred to the user -- this sandbox is headless, consistent with every prior iteration.
ALL 8 TASKS COMPLETE. Next: final whole-branch review (most-capable model) per subagent-driven-development skill.
Whole-branch review: Approved for merge (Opus reviewer, a9137aa..010083f, 9 commits). 0 Critical, 0 Important. 2 Minor (both purely cosmetic, no behavioral impact, no action taken): (1) toWhitePovEval/toWhitePovWdl use slightly different but type-identical sideToMove annotations ('w'|'b' literal vs PieceColor); (2) accuracy.ts's wdl-presence check uses truthy-array shorthand rather than an explicit != null, harmless given the w+d+l=1000 engine invariant. Reviewer independently traced the full WDL data flow end-to-end (Stockfish raw wdl output -> Rust parse/Tauri boundary -> TS api -> the ONE White-POV flip in engine-analysis.ts -> appState.wdlPerPly -> winPercentForPly -> computeGameAccuracy/classifyGame), hand-verified the POV-flip arithmetic direction is correct via worked example, confirmed winPercentForPly is genuinely the sole WDL-vs-sigmoid decision point (grepped: no other direct winPercentFromEval callers remain in computeGameAccuracy/classifyGame), confirmed every real call site of every changed signature was updated in this iteration (none left un-threaded), and confirmed the no-WDL regression-lock test reuses a byte-identical pre-existing numeric expectation.
BRANCH READY TO MERGE. Outstanding: manual GUI smoke test (real-engine WDL feeding into the Accuracy block's numbers) could not be run in this headless sandbox -- user should verify with pnpm exec tauri dev before merging. Next: use superpowers:finishing-a-development-branch.
---
Iteration 10 (Special move classes: Brilliant/Great/Miss):
Plan: docs/superpowers/plans/2026-07-20-iteration-10-special-move-classes.md
Branch: feat/special-move-classes, base 960b49c
Task 1: complete (commit 960b49c..c3720bd, review clean/Approved; MultiPV=2 requested, InfoLine.multipv + per-PV-index HashMap tracking, EngineAnalysis gains second_eval_cp/second_is_mate/second_wdl defaulting to None/false/None when no MultiPV#2 line seen; 21/21 engine tests pass)
Task 2: complete (commits 00d7870..0e31b17, review clean/Approved; second_eval_cp/second_is_mate/second_wdl threaded through AnalyzeFenResult (Rust+TS), pure passthrough via existing From impl; 38/38 Rust tests pass)
Task 3: complete (commits 0e31b17..6515745, review clean/Approved; secondEvalPerPly/secondWdlPerPly computed via existing toWhitePovEval/toWhitePovWdl, null-vs-zero distinction verified correct; 11/11 tests pass; minor deferred: no explicit assertion on secondEvalPerPly[0]===0 in the null-vs-zero regression test, code itself verified correct by inspection)
Task 4: complete (commit 6515745..376753a, review clean/Approved; materialForColor + isMaterialSacrifice pure module, verbatim to brief, no-SEE simplification disclosed in doc comment; 5/5 tests pass)
Task 5: complete (commit 376753a..71c3f4a, review clean/Approved by Opus reviewer; classifySpecial (Brilliant>Great>Miss>cutoff) added, mover-POV flip and playedIsBest indexing independently hand-verified correct, exact threshold constants confirmed, no Book/Forced scope creep; 15/15 focused + 274/274 full suite pass; minor deferred: secondLineWinPercent duplicates accuracy.ts's winPercentFromWdl/winPercentFromEval formulas rather than importing them -- plan-authored verbatim, correct today, DRY/drift-risk nit only)
Task 6: complete (commit 71c3f4a..268dd37, review clean/Approved; secondEvalPerPly/secondWdlPerPly wired into AppState (default [], reset in startReview), special inputs (fresh game.positions/moveMeta/bestMoves) passed into classifyGame in refreshRealAnalysis; staleness/freshness independently verified by reviewer; 274/274 full suite pass)
ALL 6 CODE TASKS COMPLETE. Next: Task 7 (full verification pass).
Task 7: automated verification complete (controller-run): cargo test 38/38 pass; full JS suite 50/50 files, 274/274 tests pass; pnpm check 0 errors/14 pre-existing warnings (same categories as prior iterations); pnpm lint clean; pnpm build succeeds. No fixes needed this pass. Manual GUI smoke test deferred to the user -- this sandbox is headless, consistent with every prior iteration.
ALL 7 TASKS COMPLETE. Next: final whole-branch review (most-capable model) per subagent-driven-development skill.
Whole-branch review: Approved for merge (Opus reviewer, 960b49c..268dd37, 6 commits). 0 Critical, 0 Important. Minor (all non-blocking): (1) MultiPV=2 now requested unconditionally for all analysis -- primary-line eval/accuracy/Game-Rating numbers may shift slightly vs pre-branch since MultiPV>1 disables some Stockfish search optimizations (per-spec, inherent to Task 1's design, not a bug); (2) a truly-forced (1-legal-move) position never fires Great since Stockfish emits no multipv 2 there -- explicitly sanctioned by the plan's Global Constraints; (3) secondLineWinPercent duplicates accuracy.ts's win% formulas (already recorded at Task 5, confirmed still byte-identical today). Reviewer traced the full data flow end-to-end (Stockfish multipv 2 -> Rust HashMap-by-PV-index -> Tauri IPC -> TS White-POV flip -> appState -> classifyGame's special inputs -> Brilliant/Great/Miss), confirmed every classCodes consumer (TOKENS.classification, CLASSIFICATION_ICONS, COACH_TEXT_MAP, BREAKDOWN_ORDER, NOT_BEST_CODES, NOTABLE_CODES) is exhaustive over all 10 ClassCode values via `satisfies Record<ClassCode,...>` and correctly handles the 3 new producible codes, and confirmed no Forced/Book/new-ClassCode scope creep anywhere in the branch.
BRANCH READY TO MERGE. Outstanding: manual GUI smoke test (real game with a sacrifice/only-move/missed-win moment, confirming the Breakdown table's Brilliant/Great/Miss rows show non-zero counts) could not be run in this headless sandbox -- user should verify with pnpm exec tauri dev before merging. Next: use superpowers:finishing-a-development-branch.
---
Iteration 11 (Brilliant/Great calibration fixes, using Byrne-Fischer 1956 reference game):
Plan: docs/superpowers/plans/2026-07-20-iteration-11-brilliant-great-calibration.md
Branch: feat/special-move-classes (continuing, not a new branch -- fixes Iteration 10's own not-yet-merged logic), base 257d011
Task 1: complete (commits 818cc9f..c6243a1..75bf3f6, review clean after 1 fix round; widened Brilliant sacrifice window to see material given up on opponent's next reply (offered sacrifices like 17...Be6), then tightened with a causal-attribution guard (playedMove.to must no longer hold mover's own piece) after reviewer found the naive widened diff could misattribute an unrelated capture elsewhere on the board; 18/18 focused + 277/277 full suite pass; minor deferred: guard silently no-ops if playedMove is undefined, latent since all current callers always populate moveMeta)
Task 2: complete (commit 75bf3f6..1bc3a8b, review clean/Approved; GREAT_ONLY_MOVE_GAP raised 10->20, GREAT_NOT_ALREADY_DECIDED=97 guard added mirroring BRILLIANT_NOT_WINNING, correct beforePov variable confirmed by reviewer; pre-existing Great test (beforePov=75, gap=20) still passes unmodified; 19/19 tests pass)
Task 3: complete (commit 1bc3a8b..64f5791, review clean/Approved; golden-fixture regression test for the Byrne-Fischer 17...Be6 brilliancy pattern, reviewer independently confirmed discriminating power by checking out pre-fix commit 71c3f4a and reproducing the original bug; 1/1 test passes; minor deferred: doesn't cover the causal-attribution guard's reject branch, already covered separately by Task 1's own dedicated test)
ALL 3 CODE/TEST TASKS COMPLETE. Next: Task 4 (full verification pass).
Task 4: automated verification complete (controller-run): full JS suite 51/51 files, 279/279 tests pass; pnpm check 0 errors/14 pre-existing warnings; pnpm lint clean; pnpm build succeeds. No Rust changes this iteration. No fixes needed. Manual GUI smoke test deferred to the user -- reload the reference PGN and confirm 17...Be6 shows Brilliant, Great count has dropped meaningfully.
ALL 4 TASKS COMPLETE. Next: whole-branch review (scoped to this iteration's delta) per subagent-driven-development skill.
Whole-branch review (Iteration 11 delta): Approved (Opus reviewer, 13237b7..64f5791, 6 commits). 0 Critical, 0 Important. Minor (all non-blocking): (1) material.ts's docstring was also touched (plan said "confined to classifySpecial") -- justified, a necessary correction since the widening made the old comment false, not scope creep; (2) same-ply fallback comments overstate its real-world applicability (in practice a mover never loses material on their own move, so the fallback is effectively inert on real data -- correct behavior, misleading framing only); (3) causal guard silently no-ops if playedMove is undefined while positions[ply+1] exists, already recorded as a latent Task-1 deferral; (4) secondLineWinPercent's formula duplication, already recorded from Iteration 10. Reviewer re-read the whole classifySpecial function fresh (not just incremental diffs), confirmed Brilliant>Great>Miss override order holds correctly post-fix, and confirmed the shared beforePov<97 guard between Brilliant and Great doesn't interact pathologically.
BRANCH READY (still not merged/PR'd, per the user's earlier "keep as-is" choice for Iteration 10). Outstanding: manual GUI smoke test (reload docs/references/DonaldByrne_RJamesFischer/ReferenceGame.pgn, confirm 17...Be6 now shows Brilliant and Fischer's Great count has dropped from 5) deferred to the user -- headless sandbox.
---
Iteration 12 (attack-based Brilliant detection, corrected diagnosis after real-app run showed Brilliant still 0 and Great collapsed to 0):
Plan: docs/superpowers/plans/2026-07-20-iteration-12-attack-based-brilliant.md
Branch: feat/special-move-classes (continuing), base 26d6083
Task 1: complete (commit 4832355..4e75fba, review clean/Approved after user-authorized queen-test fixture correction e8->h8; controller re-verified focused suite 16/16 passing)
Tasks 2-3: complete as one user-authorized atomic unit (commit 4e75fba..8b26096, review clean/Approved; controller re-verified combined focused suite 37/37 passing; atomic boundary avoids Task 2's otherwise broken intermediate import)
Task 4: complete (commits 8b26096..f1c9f87..bad7a72, review Approved after one user-authorized fixture-arithmetic fix round; exact 20-point Great-gap boundary and 99.25 already-decided rejection covered; controller re-verified 19/19 focused tests passing)
Task 5: automated verification complete (controller-run): full JS suite 52/52 files and 292/292 tests pass; pnpm check 0 errors/14 pre-existing warnings; pnpm lint clean; pnpm build succeeds. No verification fixes required. Manual GUI smoke test remains deferred to the user in this headless environment.
Whole-iteration review: Approved/Ready to merge for 26d6083..bad7a72. 0 Critical, 0 Important. One non-blocking Minor: the reference Na4 regression uses a White first-ply pattern and therefore does not directly lock Black-mover indexing/POV/ownership together; reviewer recommended a future ply-2 Black-pattern fixture. Explicit heuristic limitations (count-only exchanges, pins, favorable captures) remain plan-level scope, not implementation defects.

---
Value-aware special classification correction:
Task 1: complete (commits df8f900..34af919, review clean after 1 documentation/dead-code fix round; focused 24/24 and full Vitest 300/300 pass; user-authorized attacker fixture correction `['b5', 'a4']`)
Task 2: complete (commit 34af919..9913c39, review clean/Approved; focused 46/46 and full Vitest 303/303 pass)
Task 3: complete (commit 9913c39..cbe5372, golden fixture debugged via systematic-debugging against all 82 plies of the full reference game; found the plan's original Brilliant <97/no-causal-gate and Great >=20-gap thresholds over-fired on persistent-but-uncapitalized-on sacrifices and continuations inside an already-won position -- amended per docs/superpowers/plans/2026-07-20-value-aware-special-classification.md's updated Global Constraints: Brilliant tightened to <80 with a causal-sacrifice gate (moved-piece SEE exposure increased, or best move with a >=20 CP gap), Great lowered to >=15 gap with a new no-positive-exchange-target guard; classify.reference-game.test.ts now asserts exactly Fischer's 3 Brilliants ([21,29,33]) / 1 Great ([37]) with no extras; full Vitest 308/308 pass)
Task 4: automated verification complete (controller-run): pnpm exec vitest run 52/52 files, 308/308 tests; pnpm check 0 errors/14 pre-existing warnings (1 fixture-cast type error found & fixed: classify.reference-game.test.ts needed `as unknown as ReferenceAnalysisFixture` since the frozen JSON's Position values widen to string[]); pnpm lint clean; pnpm build succeeds; cd src-tauri && cargo test 38/38 pass (no Rust changes this task). codebase-memory reindex confirmed classifySpecial reaches both staticExchangeGain and hasPositiveExchangeTarget. Manual GUI confirmation against the real PGN (Step 4) deferred to the user -- headless sandbox.

---
Kasparov-Topalov overfitting-check calibration (docs/references/calibration-log.md):
User ran the real GUI against both reference games post-Task-4: Fischer game matched chess.com's Brilliant/Great exactly (0/3, 0/1) with only Book-gap-driven deviations elsewhere (documented, not actioned -- Book is out of scope). A second game (Kasparov-Topalov 1999, chosen specifically to check for overfitting) showed real Brilliant/Great mismatches. Captured a frozen, deterministic fixture the same way as Game 1's (temporary `reference_fixture_dump` Rust test appended to and then reverted out of src-tauri/src/lib.rs, ~139s real Stockfish pass, `src/lib/game/fixtures/kasparov-topalov-analysis.json`, 88 positions/87 moves). Diagnosed via a Node reimplementation of the gate logic scored against chess.com's known labels for both games simultaneously (precision/recall grid search) to avoid re-overfitting to just this one new game. Found: (1) 2 of Brilliant's 4 false positives (22...Nbxd5, 27.b4+) came from a SEE-exposure delta of only 1-2 (sub-minor-piece noise); (2) the other 2 (26.Qxd4+, 38.Bxc4) only qualify via the CP-gap causal fallback, whose false-positive gap values (29.3, 61.8) straddle Game 1's real Be6 brilliancy (28.1) -- no single threshold separates them, a genuine heuristic ceiling, not a tuning gap; (3) Great's false positive/negative set does not correlate with CP-gap magnitude at all in this game (some true Greats have near-zero gap, some false positives have large gap) -- untunable with the current 2-line signal. Fixed (1) only, since it's the one improvement that generalizes without regressing Game 1: `BRILLIANT_MIN_SACRIFICE_VALUE`-gated causal SEE delta (was: any nonzero delta) + `BRILLIANT_CAUSAL_GAP` raised 20->25 (still comfortably below Be6's 28.1). Verified via grid search against BOTH fixtures together: Fischer stays exactly 3/1 with 0 FP/FN; Kasparov Brilliant FPs drop 4->2. Added `classify.kasparov-topalov.test.ts` (permanent, not a diagnostic) locking in the 8 real matches plus a regression guard for the 2 fixed false positives -- deliberately does NOT assert full parity (documented as an accepted heuristic limit, consistent with this plan's Global Constraints against game-specific production logic). Full verification: pnpm exec vitest run 53/53 files, 317/317 tests; pnpm check 0 errors/14 pre-existing warnings; pnpm lint clean; pnpm build succeeds; src-tauri/src/lib.rs confirmed restored to a clean diff (temporary capture code fully reverted). Remaining gaps documented in docs/references/calibration-log.md, not actioned further this session (diminishing returns / fundamental signal limits, not a scope cut).

---
Phase Rating (Opening/Middlegame/Endgame, lichess Divider port):
Plan: docs/superpowers/plans/2026-07-21-phase-rating.md
Branch: feat/special-move-classes (continuing), base a5de778
Task 1: complete (commits a5de778..8a935a8, review clean after 1 lint fix round; phase.ts/phase.test.ts verified byte-identical to the brief's given code, all 3 midgame triggers + endgame-ordering + same-ply null-out edge case independently covered; 8/8 focused tests pass)
Task 2: complete (commits 8a935a8..4f3d16d, review clean/Approved; startPly mover-attribution fix verified via 2 independently-traced worked examples; only 1 pre-existing computeGameAccuracy call site confirmed unaffected; 331/331 full suite pass)
Task 3: complete (commits 4f3d16d..9bfbef7, review clean/Approved; found & fixed 1 real plan bug (this session's own brief error): dash-placeholder test asserted length 2 but the fixture's 2 null-both-sides phases produce 4 dashes (2 phases x 2 sides), corrected and independently re-verified by the reviewer; PHASE_ROWS fully removed with zero dangling references; analysisStatus==='ready' gating idiom confirmed matching the pre-existing accuracy derived value; 333/333 full suite pass)
Task 4: automated verification complete (controller-run): full JS suite 54/54 files, 333/333 tests pass; pnpm check 0 errors/14 pre-existing warnings (same categories as every prior iteration); pnpm lint clean; pnpm build succeeds. No Rust changes, no cargo test needed. No fixes required. Manual GUI confirmation (real phase badges/tooltips, short-game dash placeholder for Endgame) deferred to the user -- headless sandbox.
ALL 4 TASKS COMPLETE. Next: whole-branch review (most-capable model) per subagent-driven-development skill.
Whole-iteration review: Approved for merge (Opus reviewer, a5de778..262d2d8, 9 commits). 0 Critical, 0 Important. 1 Minor found & fixed in commit (guard getPhaseRows against Opening/Endgame ply-range overlap when middlePly is null but endPly isn't -- confirmed unreachable with this app's real PGN-parser data today, fixed defensively for a future custom-start-position source). Reviewer independently traced the full data flow end-to-end (positions -> dividePhases -> ply ranges -> computeGameAccuracy w/ startPly -> phaseBadgeCode -> PhaseRow -> PhaseTable -> ReviewTab gating), spot-checked the mixedness/score-table port against the plan's cited Scala source, verified all 4 null/non-null middlePly/endPly combinations by hand, and confirmed no comment overclaims chess.com/lichess parity beyond what's documented. Full suite 54/54 files, 333/333 tests pass; pnpm check 0 errors/14 pre-existing warnings; pnpm lint clean; pnpm build succeeds.
BRANCH READY (still not merged/PR'd, per the user's earlier "keep as-is" choice carried forward from Iteration 10). Outstanding: manual GUI check (real phase badges/tooltips on Game Review, short-game dash placeholder for Endgame) deferred to the user -- headless sandbox. Next: use superpowers:finishing-a-development-branch if/when the user wants to merge/PR the accumulated branch work.
